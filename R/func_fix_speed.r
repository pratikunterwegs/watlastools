
wat_fix_speeds <- function(data,
                           speed_cutoff) {

  # check the data
  watlastools:::wat_check_data(data,
                               names_expected = c("id", "tide_number", "patch"))

  # set order in time



}
